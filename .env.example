# JWT Viewer Configuration
# Copy this file to .env and customize as needed

# ==========================================
# Application Configuration
# ==========================================

# Application Port (inside container and local)
# Default: 8080
PORT=8080

# ==========================================
# Docker Configuration
# ==========================================

# Host Port (external access for Docker)
# This is the port you'll access on your machine
# Default: 8080
HOST_PORT=8080

# ==========================================
# Gin Framework Configuration
# ==========================================

# Gin Mode: debug or release
# - debug: Detailed logging, hot reload features
# - release: Minimal logging, optimized for production
# Default: release (in Docker), debug (local)
GIN_MODE=release

# ==========================================
# Usage Examples
# ==========================================

# Run locally on different port:
# PORT=9000 go run main.go

# Run Docker with different host port:
# HOST_PORT=8181 docker-compose up -d

# Access application:
# Local: http://localhost:${PORT}
# Docker: http://localhost:${HOST_PORT}
